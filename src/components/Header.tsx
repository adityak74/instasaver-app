import React from 'react';
import { Appbar } from 'react-native-paper';

const { Header, Content } = Appbar;

const HeaderComponent = () => (
  <Header>
    <Content title="Instasaver" />
  </Header>
);

export default HeaderComponent;
